/*
 * This convention plugin handles java conventions.
 */

plugins {
    java
}

java {
    // see kotlin conventions plugin, needs to be the same
    sourceCompatibility = JavaVersion
    targetCompatibility = JavaVersion
}
